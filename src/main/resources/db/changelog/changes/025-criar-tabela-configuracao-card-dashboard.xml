<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!-- ================================================================ -->
    <!-- TABELA: CONFIGURACAO_CARD_DASHBOARD                               -->
    <!-- Cada usuário tem suas próprias configurações de cards,            -->
    <!-- filtradas pelo perfil:                                            -->
    <!--   ADMIN_ORG / GERENTE → 7 cards                                  -->
    <!--   SUPER_ADMIN         → 7 cards (organizacao_id = NULL)          -->
    <!--   PROFISSIONAL        → 4 cards                                  -->
    <!-- ================================================================ -->
    <changeSet id="025-001-criar-tabela-configuracao-card-dashboard" author="equipe-sentinela">

        <createTable tableName="configuracao_card_dashboard">

            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>

            <!-- nullable=true: SuperAdmin não tem organização -->
            <column name="organizacao_id" type="BIGINT"/>

            <column name="usuario_id" type="BIGINT">
                <constraints nullable="false"/>
            </column>

            <column name="tipo_card" type="VARCHAR(60)">
                <constraints nullable="false"/>
            </column>

            <column name="ativo" type="BOOLEAN" defaultValueBoolean="true">
                <constraints nullable="false"/>
            </column>

            <column name="ordem_exibicao" type="INT"/>

            <column name="created_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>

            <column name="updated_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>

        </createTable>

        <!-- FKs -->
        <addForeignKeyConstraint
                baseTableName="configuracao_card_dashboard"
                baseColumnNames="organizacao_id"
                constraintName="fk_config_card_organizacao"
                referencedTableName="organizacao"
                referencedColumnNames="id"
                onDelete="CASCADE"/>

        <addForeignKeyConstraint
                baseTableName="configuracao_card_dashboard"
                baseColumnNames="usuario_id"
                constraintName="fk_config_card_usuario"
                referencedTableName="usuarios"
                referencedColumnNames="id"
                onDelete="CASCADE"/>

        <!-- Índices -->
        <createIndex tableName="configuracao_card_dashboard"
                     indexName="idx_config_card_usuario">
            <column name="usuario_id"/>
        </createIndex>

        <createIndex tableName="configuracao_card_dashboard"
                     indexName="idx_config_card_organizacao">
            <column name="organizacao_id"/>
        </createIndex>

        <createIndex tableName="configuracao_card_dashboard"
                     indexName="idx_config_card_tipo">
            <column name="tipo_card"/>
        </createIndex>

        <!-- Unique: cada usuário só pode ter uma config por tipo de card -->
        <addUniqueConstraint
                tableName="configuracao_card_dashboard"
                columnNames="usuario_id, tipo_card"
                constraintName="uk_usuario_tipo_card"/>

    </changeSet>


</databaseChangeLog>
